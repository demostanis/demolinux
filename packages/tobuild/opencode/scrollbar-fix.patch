diff -u /dev/null a/packages/core/src/renderer.ts
--- a/packages/core/src/renderer.ts
+++ b/packages/core/src/renderer.ts
@@ -1237,6 +1237,7 @@ export class CliRenderer extends EventEmitter implements RenderContext {
         if (maybeRenderable) {
           const event = new MouseEvent(maybeRenderable, { ...mouseEvent, isDragging: true })
           maybeRenderable.processMouseEvent(event)
+          ;(mouseEvent as any).stopPropagation()
         }
 
         return true
