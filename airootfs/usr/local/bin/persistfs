#!/bin/bash

export IFS=$'\n';

pfs_dir=/run/archiso/persistfs
airfs_dir=/run/archiso/airootfs
pacmandb_dir=/var/lib/pacman/local

if [ $(id -u) -ne 0 ]; then
	echo You need root >&2
	exit 1
fi

case $1 in
	add|remove|pkg|list|diff)
		exec 9>"$pfs_dir/.persistfs.lock" || exit 1
		flock -x 9 || exit 1
		;;
esac

is_adding_pkg=0

absolute_path() {
	realpath -ms -- "$1"
}

safe_path() {
	local path="$1" parent
	if [[ "$path" = / || "$path" = /index || "$path" = /index.tmp ]]; then
		printf 'Refusing reserved persistfs path: %s\n' "$path" >&2
		return 1
	fi
	parent="$(dirname -- "$path")"
	while [[ "$parent" != / ]]; do
		if [[ -L "$pfs_dir/upperdir$parent" ]]; then
			printf 'Refusing symlink ancestor in persistfs: %s\n' "$parent" >&2
			return 1
		fi
		parent="$(dirname -- "$parent")"
	done
}

add_files() {
	local file
	local -a files=()

	for file in "$@"; do
		file=$(absolute_path "$file") || return 1
		safe_path "$file" || return 1
		# Package files may have been removed by localepurge.
		[[ -e "$file" ]] || continue
		files+=( "$file" )
	done

	[[ ${#files[@]} = 0 ]] && return 0
	if ! printf '%s\n' "${files[@]}" |
		rsync -arHAXq --mkpath --files-from=- / "$pfs_dir/upperdir"; then
		return 1
	fi

	for file in "${files[@]}"; do
		if ! grep -q "$file" $pfs_dir/upperdir/index 2>/dev/null; then
			printf "%s\n" "$file" >> $pfs_dir/upperdir/index || return 1
		fi
		printf 'Added %s to persistfs.\n' "$file"
	done
}

actual_pkg() {
	pkg="${1%%=*}"
	# if the provided package name is not the actual name of the
	# package to be installed (e.g. installing xorriso actually
	# installs libisoburn since libisoburn provides xorriso),
	# find the actual name...
	pacman -Qi "$pkg" | awk -F': ' '{if(NR==1)print$2}'
}

case $1 in
	add)
		(( $# < 2 )) && echo Missing files to add >&2 && exit 1

		for file in "${@:2}"; do
			file=$(absolute_path "$file") || exit 1
			safe_path "$file" || exit 1
			if [ ! -e "$file" ]; then
				echo File does not exist: $file >&2
				exit 1
			fi
		done

		add_files "${@:2}"
		;;
	remove)
		(( $# < 2 )) && echo Missing files to remove >&2 && exit 1
		statuscode=0

		for file in "${@:2}"; do
			file=$(absolute_path "$file") || exit 1
			safe_path "$file" || exit 1
			if [ ! -L $pfs_dir/upperdir/"$file" -a ! -e $pfs_dir/upperdir/"$file" ]; then
				echo File does not exist: $file >&2
				statuscode=1
				continue
			fi

			rm -rf -- "$pfs_dir/upperdir$file" || exit 1
			grep -Fv "$file" $pfs_dir/upperdir/index > $pfs_dir/upperdir/index.tmp
			(( $? <= 1 )) || exit 1
			mv $pfs_dir/upperdir/index.tmp $pfs_dir/upperdir/index || exit 1
		done

		exit $statuscode
		;;
	pkg)
		[ -z "$2" ] && echo Missing package to install && exit 1

		is_adding_pkg=1
		add_pkg_files() {
			pkg=$(actual_pkg "$1")
			if [ -z "$pkg" ]; then
				exit 1
			fi
			echo -e Adding "\033[1;31m$pkg\033[0m" to persistfs...
			echo Computing file list...

			add_files $(pacman -Ql "$pkg" | \
				cut -d\  -f2 | \
				grep -v /$) $pacmandb_dir/$pkg-[0-9]*/{desc,files,mtree} || return 1

			unset IFS
			for dep in $(expac %D "$pkg"); do
				dep=$(actual_pkg "$dep")

				# if it's neither already in the persistfs nor in the airootfs...
				if ! compgen -G $pfs_dir/upperdir$pacmandb_dir/$dep-[0-9]* >/dev/null && \
					! compgen -G $airfs_dir$pacmandb_dir/$dep-[0-9]* >/dev/null; then
					add_pkg_files "$dep" || return 1
				fi
			done
			export IFS=$'\n'
		}

		for pkg in "${@:2}"; do
			add_pkg_files "$pkg" || exit 1
		done
		;;
	ipkg)
		[ -z "$2" ] && echo Missing package to install >&2 && exit 1

		for pkg in "${@:2}"; do
			pacman -S "$pkg" || exit 1
			"$0" pkg "$pkg" || exit 1
		done
		;;
	diff)
		[ -z "$2" ] && echo Missing file to diff >&2 && exit 1

		file="$2"
		if [ ! "${file:0:1}" = / ]; then
			file="$(pwd)/$file"
		fi

		[ ! -e $airfs_dir/"$file" ] && echo File does not exist >&2 && exit 1
		[ ! -e $pfs_dir/upperdir/"$file" ] && echo File not in persistfs >&2 && exit 1

		diff -u $airfs_dir/"$file" $pfs_dir/upperdir/"$file" | bat --file-name "$file"
		;;
	list)
		if [ "$2" = --all ]; then
			cat $pfs_dir/upperdir/index
		else
			cat $pfs_dir/upperdir/index | xargs pacman -Qo 2>&1 | grep -v $pacmandb_dir | grep 'error: No package owns' | sed 's/.*No package owns \(.*\)/\1/' | sort -u
		fi
		;;
	*)
		echo usage: "$0 [add|remove|pkg|ipkg|list] [--all]" >&2
		echo
		echo \ add: add a file to persistfs.
		echo
		echo \ remove: remove a file from persistfs.
		echo
		echo \ pkg: copy an existing pacman package to persistfs.
		echo
		echo \ ipkg: install a package with pacman -S and copy it to persistfs.
		echo
		echo \ diff: show a diff between a file in the airootfs and in the persistfs.
		echo
		echo \ list: list files in the persistfs. if --all is specified, files from packages are also listed.
		exit 1
		;;
esac
