You may use sudo pacman -S --noconfirm to install required dependencies if they're not available.

When producing simple demo web apps, do not use $ui-ux-pro-max, and launch the web server in a PTY when done, as well as executing firefox-hardened <url> &

Never start git merges. Always prefer rebases.
